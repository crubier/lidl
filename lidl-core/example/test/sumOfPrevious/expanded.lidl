((# ((# previous (a)) with behaviour ((# previous (a)) = previous (a)))+((# previous (a)) with behaviour ((# previous (a)) = previous (a)))) with behaviour ((function addition)({0:((# previous (a)) with behaviour ((# previous (a)) = previous (a)))1:((# previous (a)) with behaviour ((# previous (a)) = previous (a)))})=(# ((# previous (a)) with behaviour ((# previous (a)) = previous (a)))+((# previous (a)) with behaviour ((# previous (a)) = previous (a))))))